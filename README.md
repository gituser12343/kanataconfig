for windows, paste exe and kbd file together and double click on exe file.
for linux, 
cd ~/Downloads/kanata_linux_amd64
chmod +x kanata_linux_x64
./kanata_linux_x64 --version
sudo ./kanata_linux_x64
